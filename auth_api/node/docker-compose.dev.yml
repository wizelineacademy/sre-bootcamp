version: "3.8"

services:
  node-app:
    build:
      context: .
      args:
        NODE_ENV: "development"
    volumes:
      - ./:/app
      - node_modules:/app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev

volumes:
  node_modules:
